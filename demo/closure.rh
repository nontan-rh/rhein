
def extfun(k) {
    print(k)
}

def entry() {
    local x, varf, flit, vare
    def fun(k) { x = k }

    x = 0
    print(x)

    fun(1)
    print(x)

    varf = ^fun
    varf^(2)
    print(x)

    flit = ^(k){ x = k }
    flit^(3)
    print(x)

    vare = ^extfun
    vare^(4)
}

