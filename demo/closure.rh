
def extfun(a) {
    print(a)
}

def main() {
    local x, varf, flit, vare
    def fun(b) { x = b }

    x = 0
    print(x)

    fun(1)
    print(x)

    varf = ^fun
    varf^(2)
    print(x)

    flit = ^(k){ x = k }
    flit^(3)
    print(x)

    vare = ^extfun
    vare^(4)
}

