
defun extfun(k) {
    print(k)
}

defun entry() {
    local x, varf, flit, vare
    defun fun(k) { x = k }

    x = 0
    print(x)

    fun(1)
    print(x)

    varf = ^fun
    varf^(2)
    print(x)

    flit = ^(k){ x = k }
    flit^(3)
    print(x)

    vare = ^extfun
    vare^(4)
}

